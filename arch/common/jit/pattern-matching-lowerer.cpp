#include <jit/block-compiler.h>

using namespace captive::arch::jit;
using namespace captive::arch::x86;
using namespace captive::shared;

bool BlockCompiler::peeplower(uint32_t max_stack)
{
	dump_ir();
	
	IRInstruction *current = ctx.at(0);
	
	do {
				
	} while(true);
	
	return false;
}
